/**
 * @jsx React.DOM
 */

var React = require('react')

var notFound = React.createClass({
  render: function() {
    return (
      <div>Panda not found!</div>
    );
  }
})

module.exports = notFound
