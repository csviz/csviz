/**
 * @jsx React.DOM
 */

var React = require('react');

var Map = React.createClass({

  render: function() {
    return (
      <div>
        Map Component
      </div>
    );
  }

});

module.exports = Map;